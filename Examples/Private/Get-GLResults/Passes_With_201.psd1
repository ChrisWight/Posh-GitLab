@{
    Parameters = @{
        StatusCode = 201
    }
    Output = @{
        StatusCode = 201
        StatusText = 'Created - The POST request was successful and the resource is returned as JSON'
        Successful = $True
        Type       = 'System.Collections.Hashtable'
    }
}